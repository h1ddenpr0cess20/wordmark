# Summary
What changed and why. For bug fixes, briefly note the root cause.

## Testing
How you validated it (e.g. `npm test` / `npm run lint` / `npm run typecheck`, plus a quick browser smoke test with `npm run dev`).

## Notes (optional)
Known limitations or follow-ups.

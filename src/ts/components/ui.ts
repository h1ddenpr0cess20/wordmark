/**
 * UI components barrel.
 *
 * @remarks
 * Imported for its side effects: pulling in the UI submodules registers their
 * behavior (settings tabs, image interactions, chat messages, settings controls).
 */

import "./ui/settingsTabs.ts";
import "./ui/imageInteractions.ts";
import "./ui/chatMessages.ts";
import "./ui/settingsControls.ts";


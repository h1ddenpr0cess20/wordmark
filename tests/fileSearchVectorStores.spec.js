import test from 'node:test';
import assert from 'node:assert/strict';

// file_search and vector store tests removed — these were provider-specific features
test('placeholder: file_search tests removed', () => {
  assert.ok(true, 'file_search tests were removed');
});

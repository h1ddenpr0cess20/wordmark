import test from 'node:test';
import assert from 'node:assert/strict';

// file_search and vector store tests removed — these were OpenAI-specific features
test('placeholder: file_search tests removed (OpenAI-specific)', () => {
  assert.ok(true, 'file_search tests were removed along with OpenAI support');
});
